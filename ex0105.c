#include <stdio.h>

int main() {
    // using boolean
    int age = 16;
    int canDrive = (age >=18) ;

    printf("Age: %d\n", age);
    printf("Can drive? %d\n", canDrive);

    return 0;
}
